//div[text() = 'Apple']/parent::div/parent::div//div[@id='cell-4-undefined']

here
--> /parent:: means moving to immediate parent

//div[text()='Apple'] → finds the cell containing the fruit name.
/parent::div → moves to immediate parent div.
/parent::div → moves again to its parent row div.
//div[@id='cell-4-undefined'] → finds the Price cell within the same row.

✔️ Yes, parent:: means: move one level up in the DOM.

#extracting fruit actual price from the table
priceColumnNo = driver.find_element(By.XPATH,"//div[text()='Price']").get_attribute('data-column-id')
actual_price = driver.find_element(By.XPATH,"//div[text() ="+fruit_name+"]/parent::div/parent::div//div[@id='cell-"+priceColumnNo+"-undefined']").text
